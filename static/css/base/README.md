Base CSS

- `variables.scss`: Design tokens (colors, spacing, radii, shadows).
- `reset.scss`: Minimal reset and utilities.

Guidelines

- Keep tokens flat and semantic (no brand names).
- Prefer using CSS variables in components/layouts.
- Avoid component styles in base.

